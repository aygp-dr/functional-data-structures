#!/usr/bin/env bash

# Project configuration
export PROJECT_NAME="functional-data-structures"
export PROJECT_ROOT="$(pwd)"

# Emacs configuration
export EMACS_CONFIG="${PROJECT_ROOT}/${PROJECT_NAME}.el"

# Guile configuration
export GUILE_LOAD_PATH="${PROJECT_ROOT}/scheme:${GUILE_LOAD_PATH}"

# Python/Hy configuration  
export PYTHONPATH="${PROJECT_ROOT}/hy:${PYTHONPATH}"

# Tmux session name
export TMUX_SESSION="${PROJECT_NAME}"

# Enable direnv
echo "Project: ${PROJECT_NAME}"
echo "Root: ${PROJECT_ROOT}"